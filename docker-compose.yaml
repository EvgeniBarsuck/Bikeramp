version: '3.9'
services:
  api-db:
    image: postgres:alpine
    environment:
      POSTGRES_DB: bikeramp
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
  api:
    build:
      context: ./
      dockerfile: Dockerfile.api
    ports:
      - "3000:3000"